%table.table
  %thead
    %tr
      %th
        ID
      %th
        Name
  %tbody
    - @items.each do |item|
      %tr
        %td
          = item.id
        %td
          = item.name
= link_to 'New item', new_item_path
